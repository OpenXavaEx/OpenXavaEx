package org.openxava.demoapp.model.report;

import javax.persistence.Entity;

import org.openxava.ex.model.base.BaseDummy;

//BP: Create a mask to announce current package is "managed", because other classes in this package are not "@Entity"
@Entity
public class manageTag extends BaseDummy {
	//Do nothing
}
